import { Socket } from 'rete';

export const inputASocket = new Socket('Input A');
export const inputBSocket = new Socket('Input B');
export const outputSocket = new Socket('Output');
export const ioSocket = new Socket('IO Socket');
